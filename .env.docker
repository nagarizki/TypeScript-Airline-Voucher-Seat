# Docker Environment Variables
# These are used when running the application with docker-compose

# Database URL - SQLite file location (inside container)
DATABASE_URL=file:/app/server/data/vouchers.db

# Node environment
NODE_ENV=development

# Server port
PORT=3000
BUN_PORT=3000
